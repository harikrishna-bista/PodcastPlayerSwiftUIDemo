import XCTest
@testable import PodcastPlayerUI
import Combine

final class PodcastPlayerUITests: XCTestCase {
     
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(PodcastPlayerUI().text, "Hello, World!")
    
    }
}
